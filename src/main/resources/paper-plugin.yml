name: ScovilleCommands
description: $description
prefix: ScovilleUI
version: '$version'

main: dev.meluhdy.scovilleCommands.ScovilleCommands
api-version: '1.21.11'
load: POSTWORLD

authors: [ meluwudy ]
website: https://meluhdy.dev

dependencies:
  server:
    Melodia:
      load: BEFORE
      required: true
      join-classpath: true
    Scoville:
      load: BEFORE
      required: true
    ScovilleAchievements:
      load: BEFORE
      required: true
    ScovilleUI:
      load: BEFORE
      required: true